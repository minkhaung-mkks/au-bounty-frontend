# Loaded by `vite build` (production mode) so the map picker key bakes into
# the bundle everywhere the image is built: CI, Docker, and a plain local
# `npm run build`. Public by design (referrer-restricted in the Google
# console); the SERVER key stays in the vault and never reaches the bundle.
VITE_GOOGLE_MAPS_KEY=AIzaSyDNWDDWYUeQxbToFwz-RoqA3VthXAcpkbw
